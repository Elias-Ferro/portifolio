import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#3498db",
    },
    secondary: {
      main: "#2ecc71",
    },
    background: {
      default: "#ecf0f1",
      paper: "#fff",
    },
    text: {
      primary: "#2c3e50",
    },
  },
});
